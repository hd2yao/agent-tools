# Codex 工作台账号整合任务拆分

- [x] **T001 固定基线与同步 v0.10.1**
  映射：AC-PI-001、AC-PI-012、AC-PI-013
  验收：冷备 App 身份被记录；工作台分支包含凭据刷新保护；Python 和现有 Swift 测试通过。
  验证：版本 / hash、`python3 -m unittest -v`、`./codex-workbench/test.sh`。

- [x] **T002 完整账号 payload 与菜单展示契约**
  映射：AC-PI-002 至 AC-PI-006、AC-PI-008、AC-PI-010
  验收：Swift 可解码 runtime、账号、额度、逐张重置卡、用量和排行；当前账号和菜单栏文案由纯逻辑生成。
  验证：真实脱敏 fixture RED/GREEN 测试。

- [x] **T003 安装资源与后端 freshness**
  映射：AC-PI-012、AC-PI-016
  验收：构建只复制受控 Python 资源，指纹覆盖实际打包内容；陈旧资源 verifier 失败。
  验证：脚本测试、构建、篡改 fixture、codesign。

- [ ] **T004 切换后验证与账号协调**
  映射：AC-PI-002、AC-PI-007、AC-PI-010、AC-PI-013
  验收：切换阶段明确，命令完成后强制验证目标账号和托管状态；失败不写成功日志。
  验证：切换 verifier、AppModel fixture 和错误边界测试。

- [ ] **T005 运行状态与冷备进程互斥**
  映射：AC-PI-001、AC-PI-003、AC-PI-008、AC-PI-010
  验收：工作台使用现有 running / waiting / idle 语义；检测旧 App 后暂停自动化并提示。
  验证：状态 fixture、进程检测注入测试、菜单栏 presentation 测试。

- [ ] **T006 重置卡通知与自动重置状态机**
  映射：AC-PI-009、AC-PI-013、AC-PI-016
  验收：提醒、耗尽条件、最早到期、幂等、重试和终态与旧工具一致；到期本身不消费。
  验证：纯策略 RED/GREEN、旧偏好域测试、通知请求测试、后端命令契约。

- [ ] **T007 动态菜单栏整合**
  映射：AC-PI-003、AC-PI-004、AC-PI-008、AC-PI-010、AC-PI-014
  验收：状态项显示当前账号额度和工作状态；面板具备额度、重置卡摘要、切换、最近操作和快捷入口。
  验证：源码行为测试、AX 检查、新安装浅色 / 深色截图。

- [ ] **T008 账号管理页重构**
  映射：AC-PI-002、AC-PI-005、AC-PI-007、AC-PI-010、AC-PI-014
  验收：当前账号优先、两档额度、逐张重置卡、账号用量、其他账号切换和高级诊断完整。
  验证：三档窗口、长文本、暂存 / 错误 / 切换中截图与 AX。

- [ ] **T009 项目分析与工具 / Skill 模块**
  映射：AC-PI-006、AC-PI-010、AC-PI-014
  验收：两个真实数据模块进入侧栏，空 / 错 / 数据状态完整，不再依赖旧账号面板。
  验证：payload fixture、默认窗口截图和无横向滚动检查。

- [ ] **T010 登录启动辅助入口**
  映射：AC-PI-011、AC-PI-012
  验收：手动打开显示主窗口；登录辅助启动只驻留菜单栏；再次手动打开能显示窗口。
  验证：launcher 参数测试、bundle 结构、SMAppService 状态和本机行为检查。

- [ ] **T011 操作日志回归与账号事件**
  映射：AC-PI-007、AC-PI-009、AC-PI-013
  验收：验证后的切换、自动重置和数据源事件正确；V1.4 解释无回归。
  验证：Core 全量测试、真实 ledger 最新视图反查。

- [ ] **T012 安装与真实双向切换验收**
  映射：AC-PI-001 至 AC-PI-013、AC-PI-015、AC-PI-016
  验收：新安装工作台读取真实两个账号，完成双向切换并恢复初始账号；旧 App 仍安装但退出。
  验证：安装身份、真实 payload、Codex 登录状态、进程清单和操作日志。

- [ ] **T013 视觉、性能与收敛**
  映射：AC-PI-003 至 AC-PI-006、AC-PI-010、AC-PI-014、AC-PI-015
  验收：全部截图和 Visual Verdict 通过；内存稳定；用户可查看迁移成果；未删除冷备。
  验证：fresh build 截图、AX、RSS 观察、AC 对照和 focused diff。

- [ ] **T014 文档与长期记录**
  映射：AC-PI-001 至 AC-PI-016
  验收：README、DESIGN、安装说明、任务状态和 Obsidian Codex 变更日志与真实产物一致。
  验证：文档复核、工作树状态和最终提交范围。
