# Codex 观测站任务拆分

- [x] **T001 项目骨架与构建契约**
  映射：AC-001、AC-011  
  验收：Swift package 可测试，可封装为正常 `.app`。  
  验证：`swift test`、`./build-app.sh`、`plutil -lint`。

- [x] **T002 事件模型与 JSONL 仓库**
  映射：AC-003、AC-004、AC-006、AC-009、AC-012  
  验收：支持解码、坏行容错、倒序、日期分组、筛选、去重和脱敏字段。  
  验证：Core 单元测试。

- [x] **T003 本地证据补扫**
  映射：AC-003、AC-004、AC-009  
  验收：可从已有 ledger/context card/reset outcome 生成标注证据与置信度的事件。  
  验证：fixture 测试和本机只读 dry-run。

- [x] **T004 账号模块适配器**
  映射：AC-007、AC-012  
  验收：读取打包后的 Profile Switcher payload，展示状态并通过既有 `app <profile>` 路径切换。  
  验证：payload 解码测试、命令参数契约测试、手动状态加载。

- [x] **T005 原生 App Shell 与 Design System**
  映射：AC-002、AC-010、AC-011  
  验收：侧栏/toolbar/内容表面满足 `DESIGN.md`，窗口三档响应稳定。  
  验证：编译、最小/默认/宽屏截图。

- [x] **T006 概览、日志、账号三页**
  映射：AC-002 至 AC-007、AC-009 至 AC-011  
  验收：三页真实可用，日志可筛选、选择和展开，账号状态不混淆。  
  验证：Core 测试、交互检查、页面截图。

- [x] **T007 菜单栏、Codex 联动与任务深链**
  映射：AC-005、AC-008  
  验收：菜单栏快捷面板、打开 Codex、打开观测站、有效任务跳转可用。
  验证：URL 构造测试和本机交互验证。

- [x] **T008 安装、文档、视觉与收敛验收**
  映射：AC-001 至 AC-012  
  验收：安装到用户 Applications，产物身份可核验，Visual Verdict ≥ 90。  
  验证：全量测试、freshness 验证、浅/深色截图、AC 对照表。

- [ ] **T009 额度状态差异与官方通知监听**
  映射：AC-014
  验收：正常刷新、官方恢复、本地重置与重置次数变化可区分，当前账号近实时、所有账号 60 秒补扫。
  验证：Core RED/GREEN 测试、通知 fixture、相同快照去重。

- [ ] **T010 项目、对话与全局工作流证据**
  映射：AC-013、AC-015
  验收：线程标题和项目首屏可见；新项目、接续、摘要和受控工作流变更写入日志；普通新对话不写。
  验证：SQLite/文件指纹 fixture、本机 dry-run、隐私字段检查。

- [ ] **T011 时间轴层级、可信度与命名图标**
  映射：AC-001、AC-010、AC-011、AC-013、AC-016
  验收：`Codex 观测站.app` 具有正式图标；scope chips、重要性和可信度解释在三档窗口稳定。
  验证：构建安装、浅/深色真实截图、可访问性检查、Visual Verdict。

- [ ] **T012 V1.1 收敛与长期记录**
  映射：AC-001 至 AC-016
  验收：全量验证、安装产物身份、Obsidian 变更日志和 AC 收敛结果齐全。
  验证：`./test.sh`、`./verify-install.sh`、focused diff、文档复核。
