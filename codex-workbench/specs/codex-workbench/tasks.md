# Codex 观测站任务拆分

- [x] **T001 项目骨架与构建契约**
  映射：AC-001、AC-011  
  验收：Swift package 可测试，可封装为正常 `.app`。  
  验证：`swift test`、`./build-app.sh`、`plutil -lint`。

- [x] **T002 事件模型与 JSONL 仓库**
  映射：AC-003、AC-004、AC-006、AC-009、AC-012  
  验收：支持解码、坏行容错、倒序、日期分组、筛选、去重和脱敏字段。  
  验证：Core 单元测试。

- [x] **T003 本地证据补扫**
  映射：AC-003、AC-004、AC-009  
  验收：可从已有 ledger/context card/reset outcome 生成标注证据与置信度的事件。  
  验证：fixture 测试和本机只读 dry-run。

- [x] **T004 账号模块适配器**
  映射：AC-007、AC-012  
  验收：读取打包后的 Profile Switcher payload，展示状态并通过既有 `app <profile>` 路径切换。  
  验证：payload 解码测试、命令参数契约测试、手动状态加载。

- [x] **T005 原生 App Shell 与 Design System**
  映射：AC-002、AC-010、AC-011  
  验收：侧栏/toolbar/内容表面满足 `DESIGN.md`，窗口三档响应稳定。  
  验证：编译、最小/默认/宽屏截图。

- [x] **T006 概览、日志、账号三页**
  映射：AC-002 至 AC-007、AC-009 至 AC-011  
  验收：三页真实可用，日志可筛选、选择和展开，账号状态不混淆。  
  验证：Core 测试、交互检查、页面截图。

- [x] **T007 菜单栏、Codex 联动与任务深链**
  映射：AC-005、AC-008  
  验收：菜单栏快捷面板、打开 Codex、打开观测站、有效任务跳转可用。
  验证：URL 构造测试和本机交互验证。

- [x] **T008 安装、文档、视觉与收敛验收**
  映射：AC-001 至 AC-012  
  验收：安装到用户 Applications，产物身份可核验，Visual Verdict ≥ 90。  
  验证：全量测试、freshness 验证、浅/深色截图、AC 对照表。

- [x] **T009 额度状态差异与官方通知监听**
  映射：AC-014
  验收：正常刷新、官方恢复、本地重置与重置次数变化可区分，当前账号近实时、所有账号 60 秒补扫。
  验证：Core RED/GREEN 测试、通知 fixture、相同快照去重。

- [x] **T010 项目、对话与全局工作流证据**
  映射：AC-013、AC-015
  验收：线程标题和项目首屏可见；新项目、接续、摘要和受控工作流变更写入日志；普通新对话不写。
  验证：SQLite/文件指纹 fixture、本机 dry-run、隐私字段检查。

- [x] **T011 时间轴层级、可信度与命名图标**
  映射：AC-001、AC-010、AC-011、AC-013、AC-016
  验收：`Codex 观测站.app` 具有正式图标；scope chips、重要性和可信度解释在三档窗口稳定。
  验证：构建安装、V1.1 浅色三档与 V1 深色回归、可访问性检查、Visual Verdict。

- [x] **T012 V1.1 收敛与长期记录**
  映射：AC-001 至 AC-016
  验收：全量验证、安装产物身份、Obsidian 变更日志和 AC 收敛结果齐全。
  验证：`./test.sh`、`./verify-install.sh`、focused diff、文档复核。

- [x] **T013 Automation 脱敏语义快照与差异**
  映射：AC-017、AC-019
  验收：字段和能力标签变化生成可读摘要；完整 prompt 不进入事件或观察状态。
  验证：旧状态解码、Automation fixture、隐私断言和 RED/GREEN 测试。

- [x] **T014 来源与目标对话证据关联**
  映射：AC-018、AC-019
  验收：结构化工具调用唯一匹配时关联来源线程；目标线程独立展示；模糊匹配不写入。
  验证：rollout fixture、时间窗边界、线程目录 fixture。

- [x] **T015 追加式历史事件回填**
  映射：AC-020
  验收：同 ID 增强 revision 幂等追加，Repository 读取增强版，坏行仍保留。
  验证：ledger revision 测试和真实 11:04 事件 dry-run。

- [x] **T016 时间轴与详情信息层级**
  映射：AC-017 至 AC-020
  验收：列表一眼看到改动摘要与来源；详情按改动、归属、来源、技术证据排列。
  验证：900×640、1160×780、1440×900 真实截图与 AX 标签。

- [x] **T017 V1.2 安装、回流与收敛**
  映射：AC-001 至 AC-020
  验收：最终安装产物、真实历史事件、文档和 Obsidian 记录一致。
  验证：全量测试、构建、安装指纹、focused diff 和 converge 复核。

- [ ] **T018 跨类型安全语义快照**
  映射：AC-021、AC-022
  验收：Skill、Hook、Automation 均保存脱敏用途/能力元数据，不保存正文。
  验证：collector fixture、旧状态解码和隐私断言。

- [ ] **T019 工作流差异与降级摘要**
  映射：AC-021 至 AC-023
  验收：精确前后证据显示新增/移除；无旧证据显示更新后职责和证据边界。
  验证：事件 factory RED/GREEN 测试。

- [ ] **T020 变量式 Automation 与历史事件回填**
  映射：AC-024
  验收：`cfg.id` / `updatedPrompt` 调用可关联，截图中的通用历史事件幂等增强。
  验证：真实结构 fixture、真实 ledger dry-run、重复运行不增行。

- [ ] **T021 时间轴语义层级回归**
  映射：AC-021 至 AC-024
  验收：列表先说明改动，详情先显示语义变化和证据边界，指纹仅在技术状态。
  验证：900×640、1160×780、1440×900 截图与可访问性检查。

- [ ] **T022 V1.3 安装、回流与收敛**
  映射：AC-001 至 AC-024
  验收：测试、安装产物、真实事件、文档和 Obsidian 记录一致。
  验证：全量测试、安装 freshness、focused diff 和 AC 对照。
